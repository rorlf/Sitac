@model IEnumerable<testando.Models.protocolo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ano)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.data_cadastro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.data_prazo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.protocolo_status_id)
        </th>
        <th></th>
    </tr>

    
 @foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ano)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.data_cadastro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.data_prazo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.protocolo_status_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
            @Html.ActionLink("Details", "Details", new { id = item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.id })
        </td>
    </tr>
}
    
</table>
